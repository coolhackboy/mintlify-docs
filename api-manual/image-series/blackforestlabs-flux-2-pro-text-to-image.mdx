---
title: "FLUX.2 Pro Text to Image"
description: "Create detailed images with FLUX.2 Pro, using natural language to direct subjects, composition, and visible text at up to 2K."
icon: "image"
openapi: "/api-manual/image-series/blackforestlabs-flux-2-pro-text-to-image.json POST /api/generate/submit"
---

<Tip>
1. Submit the request with `VIDGO_API_KEY`. A `task_id` is returned immediately. Keep it until the task reaches `finished` or `failed`.
2. Use [Query Task Status](/api-manual/task-management/status) to retrieve the result. If you provide a `callback_url`, Vidgo sends the result to your [webhook](/api-manual/task-management/webhooks) when the task finishes or fails.
</Tip>

# FLUX.2 Pro Text to Image

Create detailed images with FLUX.2 Pro, using natural language to direct subjects, composition, and visible text at up to 2K.

## Available Models

- **blackforestlabs/flux-2-pro/text-to-image** - Generate images from text prompts.

## Output Options

### Image Size

- `size`: Required. Accepted values: `1:1`, `4:3`, `3:4`, `16:9`, `9:16`, `3:2`, `2:3`, `auto`.

### Resolution

- `resolution`: Required. Accepted values: `1K`, `2K`.

## Key Features

- Generate images from text prompts.
- Choose from `1K`, `2K` output resolutions.

## Advanced Parameters

Set `model` and optional `callback_url` at the request root. Place all workflow parameters, including output options, inside `input`.

Send the input fields listed on this page.

### Prompt

- `prompt`: Required. Describe the desired image. Length: **3-5,000 characters**.

## Verified Example

This example was generated through the Vidgo API and is also the default example in the playground. Source and result images are hosted on the Vidgo CDN.

![Night Ferry in the Rain](https://cdn.vidgo.ai/apis/models/blackforestlabs/flux-2-pro/text-to-image/v1/01/output.jpg)

```json
{
  "code": 200,
  "data": {
    "task_id": "FUI8N7NW2JLV34RB",
    "status": "finished",
    "files": [
      {
        "file_type": "image",
        "file_url": "https://cdn.vidgo.ai/apis/models/blackforestlabs/flux-2-pro/text-to-image/v1/01/output.jpg"
      }
    ],
    "created_time": "2026-09-26T10:30:10",
    "error_message": null,
    "progress": 100
  }
}
```
