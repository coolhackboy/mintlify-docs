---
title: "Grok Imagine Image Quality Edit"
description: "Develop new image variations with Grok Imagine Image Quality, combining text instructions and visual references in output up to 2K."
icon: "image"
openapi: "/api-manual/image-series/xai-grok-imagine-image-quality-edit.json POST /api/generate/submit"
---

<Tip>
1. Submit the request with `VIDGO_API_KEY`. A `task_id` is returned immediately. Keep it until the task reaches `finished` or `failed`.
2. Use [Query Task Status](/api-manual/task-management/status) to retrieve the result. If you provide a `callback_url`, Vidgo sends the result to your [webhook](/api-manual/task-management/webhooks) when the task finishes or fails.
</Tip>

# Grok Imagine Image Quality Edit

Develop new image variations with Grok Imagine Image Quality, combining text instructions and visual references in output up to 2K.

## Available Models

- **xai/grok-imagine-image-quality/edit** - Edit images using a prompt and image references.

## Output Options

### Aspect Ratio

- `aspect_ratio`: Optional. Accepted values: `1:1`, `2:3`, `3:2`, `9:16`, `16:9`. Default: `1:1`.

### Resolution

- `resolution`: Optional. Accepted values: `1K`, `2K`. Default: `1K`.

### Image Count

- `n`: Optional. Number of images to generate. Integer, at least `1`. Default: `1`. No maximum is documented upstream; fractional numbers, strings, and booleans are rejected.

## Key Features

- Edit images using a prompt and image references.
- Accepts 1-3 images in `image_urls`.
- Choose from `1K`, `2K` output resolutions.
- Control the number of output images with `n`.

## Advanced Parameters

Set `model` and optional `callback_url` at the request root. Place all workflow parameters, including output options, inside `input`.

Send only the input fields supported by this workflow. Unsupported `input` fields are rejected before credits are deducted. Input URLs must use HTTP(S), include a hostname, and contain no embedded credentials.

### Prompt

- `prompt`: Required. Describe the requested changes. Minimum length: **1 character**.
- The value must be a string and must not be blank. No maximum prompt length is documented upstream.

### Image URLs

- `image_urls`: Required. Provide publicly accessible image URLs. Supply 1-3 items. Each item must be an HTTP(S) URL.

## Pricing

| Usage | Credits | Price | Official comparison |
| --- | ---: | ---: | ---: |
| 1K output image | 8 | $0.040 / image | $0.050 / image |
| 2K output image | 11 | $0.055 / image | $0.070 / image |
| Reference image (Edit only; additional fee) | 2 | $0.010 / image | $0.010 / image |

Total credits = output count (`n`) × resolution rate + reference image count × 2.
Reference image fees are charged once per request, not once per output. Text-to-image has no reference image fee.
1 credit = $0.005. For an Edit request, 2 outputs at 2K with 3 references cost 28 credits ($0.140).
Comparison rates are the supplied official price snapshot; savings round to 20% (1K) and 21% (2K).

Credits are deducted on submission and refunded if the task fails.
