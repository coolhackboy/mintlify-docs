---
title: "FLUX.2 Flex Edit"
description: "Use FLUX.2 Flex to rework images or combine references, guiding the subject, style, and composition through text."
icon: "image"
openapi: "/api-manual/image-series/blackforestlabs-flux-2-flex-edit.json POST /api/generate/submit"
---

<Tip>
1. Submit the request with `VIDGO_API_KEY`. A `task_id` is returned immediately. Keep it until the task reaches `finished` or `failed`.
2. Use [Query Task Status](/api-manual/task-management/status) to retrieve the result. If you provide a `callback_url`, Vidgo sends the result to your [webhook](/api-manual/task-management/webhooks) when the task finishes or fails.
</Tip>

# FLUX.2 Flex Edit

Use FLUX.2 Flex to rework images or combine references, guiding the subject, style, and composition through text.

## Available Models

- **blackforestlabs/flux-2-flex/edit** - Edit images using a prompt and image references.

## Output Options

### Image Size

- `size`: Required. Accepted values: `1:1`, `4:3`, `3:4`, `16:9`, `9:16`, `3:2`, `2:3`, `auto`. `auto` uses the aspect ratio of the first image in `image_urls`.

### Resolution

- `resolution`: Required. Accepted values: `1K`, `2K`.

## Key Features

- Edit images using a prompt and image references.
- Accepts 1-8 images in `image_urls`.
- Choose from `1K`, `2K` output resolutions.

## Advanced Parameters

Set `model` and optional `callback_url` at the request root. Place all workflow parameters, including output options, inside `input`.

Send the input fields listed on this page. Each input URL is a public HTTP(S) URL.

### Prompt

- `prompt`: Required. Describe the requested changes. Length: **3-5,000 characters**.

### Image URLs

- `image_urls`: Required. Provide publicly accessible image URLs. Supply 1-8 items. Each item must be an HTTP(S) URL.

## Verified Example

This example was generated through the Vidgo API and is also the default example in the playground. Source and result images are hosted on the Vidgo CDN.

![Reference image 1](https://cdn.vidgo.ai/apis/models/blackforestlabs/flux-2-flex/edit/v1/01/input-01.png)

![Reference image 2](https://cdn.vidgo.ai/apis/models/blackforestlabs/flux-2-flex/edit/v1/01/input-02.png)

![Reference image 3](https://cdn.vidgo.ai/apis/models/blackforestlabs/flux-2-flex/edit/v1/01/input-03.png)

![Pip and the Glass Garden](https://cdn.vidgo.ai/apis/models/blackforestlabs/flux-2-flex/edit/v1/01/output.jpg)

```json
{
  "code": 200,
  "data": {
    "task_id": "E4MB5ERGH360DFJI",
    "status": "finished",
    "files": [
      {
        "file_type": "image",
        "file_url": "https://cdn.vidgo.ai/apis/models/blackforestlabs/flux-2-flex/edit/v1/01/output.jpg"
      }
    ],
    "created_time": "2026-09-26T11:07:59",
    "error_message": null,
    "progress": 100
  }
}
```
