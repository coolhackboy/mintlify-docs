---
title: "Seedream 4.5 Text to Image"
description: "Develop visual concepts with Seedream 4.5, generating multiple images from a prompt to explore different styles and compositions."
icon: "image"
openapi: "/api-manual/image-series/bytedance-seedream-v4-5-text-to-image.json POST /api/generate/submit"
---

<Tip>
1. Submit the request with `VIDGO_API_KEY`. A `task_id` is returned immediately. Keep it until the task reaches `finished` or `failed`.
2. Use [Query Task Status](/api-manual/task-management/status) to retrieve the result. If you provide a `callback_url`, Vidgo sends the result to your [webhook](/api-manual/task-management/webhooks) when the task finishes or fails.
</Tip>

# Seedream 4.5 Text to Image

Develop visual concepts with Seedream 4.5, generating multiple images from a prompt to explore different styles and compositions.

## Available Models

- **bytedance/seedream/v4.5/text-to-image** - Generate images from text prompts.

## Output Options

### Image Size

- `size`: Optional. Default: `1:1`.
- Presets: `2K`, `4K`, `1:1`, `4:3`, `3:4`, `16:9`, `9:16`, `3:2`, `2:3`, `21:9`.
- For custom dimensions, supply an object with integer `width` and `height`; both must be positive.
- Custom dimensions also accept a `WIDTHxHEIGHT` string, using `x` or `X` between the positive dimensions.

### Image Count

- `n`: Optional. Number of images to generate. Integer from `1` to `15`. Default: `1`.

## Key Features

- Generate images from text prompts.
- Control the number of output images with `n`.

## Advanced Parameters

Set `model` and optional `callback_url` at the request root. Place all workflow parameters, including output options, inside `input`.

### Prompt

- `prompt`: Required. Describe the desired image. Length: **1-3,000 characters**.
- Include at least one non-whitespace character.

### Safety Checker

- `enable_safety_checker`: Optional. Set `true` to enable the content safety check or `false` to disable it. Default: `true`.

## Verified Example

This example was generated through the Vidgo test API with the `basic` request shown in the API playground. The result image is hosted on the Vidgo CDN.

![Aurora Ridge Station Cutaway](https://cdn.vidgo.ai/apis/models/bytedance/seedream/v4.5/text-to-image/v1/01/output.jpg)

```json
{
  "code": 200,
  "data": {
    "task_id": "PGQCZ3X31DC6OEXQ",
    "status": "finished",
    "files": [
      {
        "file_url": "https://cdn.vidgo.ai/apis/models/bytedance/seedream/v4.5/text-to-image/v1/01/output.jpg",
        "file_type": "image"
      }
    ],
    "created_time": "2026-09-25T16:14:03",
    "error_message": null,
    "progress": 100
  }
}
```
