---
title: "Kling 3.0 Standard Image to Video"
description: "Animate image references with Kling 3.0 Standard, with support for multiple shots, individual shot prompts, and optional sound."
icon: "video"
openapi: "/api-manual/video-series/kwaivgi-kling-v3-0-std-image-to-video.json POST /api/generate/submit"
---

## Usage Guide

Submit `kwaivgi/kling-v3.0-std/image-to-video` with `VIDGO_API_KEY`. Keep model and callback_url at the request root and place workflow parameters inside input.

The response contains a task_id. Use [Query Task Status](/api-manual/task-management/status) or [Webhook Callbacks](/api-manual/task-management/webhooks) to retrieve the files.

## Parameter Details

Required input fields: `duration`, `image_urls`.

- Send only the fields shown for this workflow. Parameters from other workflows are rejected before credits are deducted.
- When supplied, prompt and text must not be blank. Input URL fields require HTTP(S) URLs with a hostname and no embedded credentials.
- Each multi_prompt shot needs a nonblank prompt and positive integer duration. Shot durations must sum to the requested duration. When multi_shots is available, enable it with multi_prompt; multi_shots=true also requires multi_prompt.
- kling_elements requires exactly one starting image.

## Developer Notes

Keep the task_id until the task reaches finished or failed. Failed tasks return charged credits through the normal refund process. Existing public IDs remain available; use the exact workflow ID shown on this page for new integrations.

## Optional parameters

Available input fields: `prompt`, `multi_prompt`, `multi_shots`, `sound`, `kling_elements`, `aspect_ratio`. See the request schema for accepted values and defaults.
