---
title: "Kling Avatar 2.0 Pro"
description: "Create a speaking avatar with Kling Avatar 2.0 Pro, combining a character image and an audio performance with optional text direction."
icon: "video"
openapi: "/api-manual/video-series/kwaivgi-kling-video-ai-avatar-v2-pro.json POST /api/generate/submit"
---

## Usage Guide

Submit `kwaivgi/kling-video/ai-avatar/v2/pro` with `VIDGO_API_KEY`. Keep model and callback_url at the request root and place workflow parameters inside input.

The response contains a task_id. Use [Query Task Status](/api-manual/task-management/status) or [Webhook Callbacks](/api-manual/task-management/webhooks) to retrieve the files.

## Parameter Details

Required input fields: `audio_url`, `image_urls`.

- Send only the fields shown for this workflow. Parameters from other workflows are rejected before credits are deducted.
- When supplied, prompt and text must not be blank. Input URL fields require HTTP(S) URLs with a hostname and no embedded credentials.
- Driving audio must be between two and sixty seconds. Usage is based on its detected duration, rounded up to a whole second.
- Media inspected for duration must be a publicly reachable HTTP(S) file of at most 100 MiB.

## Developer Notes

Keep the task_id until the task reaches finished or failed. Failed tasks return charged credits through the normal refund process. Existing public IDs remain available; use the exact workflow ID shown on this page for new integrations.

## Optional parameters

Available input fields: `prompt`. See the request schema for accepted values and defaults.
