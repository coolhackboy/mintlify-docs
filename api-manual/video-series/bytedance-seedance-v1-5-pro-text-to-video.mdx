---
title: "Seedance 1.5 Pro Text to Video"
description: "Create video scenes with Seedance 1.5 Pro, with optional generated audio and a fixed-camera setting for controlled compositions."
icon: "video"
openapi: "/api-manual/video-series/bytedance-seedance-v1-5-pro-text-to-video.json POST /api/generate/submit"
---

## Usage Guide

Submit `bytedance/seedance-v1.5-pro/text-to-video` with `VIDGO_API_KEY`. Keep model and callback_url at the request root and place workflow parameters inside input.

The response contains a task_id. Use [Query Task Status](/api-manual/task-management/status) or [Webhook Callbacks](/api-manual/task-management/webhooks) to retrieve the files.

## Parameter Details

Required input fields: `aspect_ratio`, `duration`, `generate_audio`, `prompt`, `resolution`.

- Send only the fields shown for this workflow. Parameters from other workflows are rejected before credits are deducted.
- When supplied, prompt and text must not be blank. Input URL fields require HTTP(S) URLs with a hostname and no embedded credentials.

## Developer Notes

Keep the task_id until the task reaches finished or failed. Failed tasks return charged credits through the normal refund process. Existing public IDs remain available; use the exact workflow ID shown on this page for new integrations.

## Optional parameters

Available input fields: `fixed_lens`. See the request schema for accepted values and defaults.
