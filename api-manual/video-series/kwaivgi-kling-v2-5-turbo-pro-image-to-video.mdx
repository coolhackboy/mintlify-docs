---
title: "Kling 2.5 Turbo Pro Image to Video"
description: "Animate a starting image with Kling 2.5 Turbo Pro, with an optional closing frame to guide the end of the shot."
icon: "video"
openapi: "/api-manual/video-series/kwaivgi-kling-v2-5-turbo-pro-image-to-video.json POST /api/generate/submit"
---

## Usage Guide

Submit `kwaivgi/kling-v2.5-turbo-pro/image-to-video` with `VIDGO_API_KEY`. Keep model and callback_url at the request root and place workflow parameters inside input.

The response contains a task_id. Use [Query Task Status](/api-manual/task-management/status) or [Webhook Callbacks](/api-manual/task-management/webhooks) to retrieve the files.

## Parameter Details

Required input fields: `prompt`, `start_image_url`.

- Send only the fields shown for this workflow. Parameters from other workflows are rejected before credits are deducted.
- When supplied, prompt and text must not be blank. Input URL fields require HTTP(S) URLs with a hostname and no embedded credentials.

## Developer Notes

Keep the task_id until the task reaches finished or failed. Failed tasks return charged credits through the normal refund process. Existing public IDs remain available; use the exact workflow ID shown on this page for new integrations.

## Optional parameters

Available input fields: `duration`, `end_image_url`, `aspect_ratio`, `negative_prompt`. See the request schema for accepted values and defaults.
