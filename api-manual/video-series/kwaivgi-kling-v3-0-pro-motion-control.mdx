---
title: "Kling 3.0 Pro Motion Control"
description: "Transfer movement from a reference video to an image subject with Kling 3.0 Pro, with orientation control and 1080p output."
icon: "video"
openapi: "/api-manual/video-series/kwaivgi-kling-v3-0-pro-motion-control.json POST /api/generate/submit"
---

## Usage Guide

Submit `kwaivgi/kling-v3.0-pro/motion-control` with `VIDGO_API_KEY`. Keep model and callback_url at the request root and place workflow parameters inside input.

The response contains a task_id. Use [Query Task Status](/api-manual/task-management/status) or [Webhook Callbacks](/api-manual/task-management/webhooks) to retrieve the files.

## Parameter Details

Required input fields: `character_orientation`, `image_urls`, `video_urls`.

- Send only the fields shown for this workflow. Parameters from other workflows are rejected before credits are deducted.
- When supplied, prompt and text must not be blank. Input URL fields require HTTP(S) URLs with a hostname and no embedded credentials.
- This ID fixes `resolution` to `1080p`.
- Input video must be at least three seconds. The maximum is ten seconds with image orientation or thirty seconds with video orientation. Facial elements require video orientation.
- Media inspected for duration must be a publicly reachable HTTP(S) file of at most 100 MiB.

## Developer Notes

Keep the task_id until the task reaches finished or failed. Failed tasks return charged credits through the normal refund process. Existing public IDs remain available; use the exact workflow ID shown on this page for new integrations.

## Optional parameters

Available input fields: `prompt`, `resolution`, `kling_elements`. See the request schema for accepted values and defaults.
