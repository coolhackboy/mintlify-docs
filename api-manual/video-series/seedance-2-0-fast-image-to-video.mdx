---
title: "Seedance 2.0 Fast Image to Video"
description: "Seedance 2.0 Fast image to video model with multimodal generation and optional audio"
icon: "video"
openapi: "/api-manual/video-series/seedance-2-0-fast-image-to-video.json POST /api/generate/submit"
---

<Tip>
1. Submit the request with `VIDGO_API_KEY`. After submission, a `task_id` is returned. If `callback_url` is provided, Vidgo sends the result after the task finishes or fails.
2. You can always retrieve the result through the unified [Query Task Status](/api-manual/task-management/status) endpoint.
</Tip>

## Available Models

- **seedance-2.0-fast/image-to-video** - Seedance 2.0 Fast image to video generation

## Key Features

- **Image To Video**: Control the starting frame and optional ending frame with up to two images.
- **Optional Audio**: Request generated audio together with the video.

## Variant Notes

- This public model ID uses the `seedance-2-fast` provider variant.
- Supported resolutions are `480p` and `720p`.

## Important Notes

- `prompt` is required and accepts up to 20,000 characters.
- `duration` supports integer values from `4` to `15`.
- `image_urls` accepts one starting frame and one optional ending frame; reference fields are not accepted.
- `aspect_ratio` accepts `auto`, `21:9`, `16:9`, `4:3`, `1:1`, `3:4`, or `9:16`.
- `image_urls` is required and cannot be combined with reference-media fields.
- All remote URLs must be publicly accessible and directly downloadable.

## Credits Reference

- Usage is calculated from output duration and selected resolution.
- Check the Vidgo dashboard for the current model rate before submitting production traffic.
