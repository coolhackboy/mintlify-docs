---
title: "Gemini Omni 1.1 Flash Reference to Video"
description: "Use visual references to guide new scenes with Gemini Omni 1.1 Flash, combining image input and text direction in video output up to 4K."
icon: "video"
openapi: "/api-manual/video-series/google-gemini-omni-1-1-flash-reference-to-video.json POST /api/generate/submit"
---

## Usage Guide

Submit `google/gemini-omni-1.1-flash/reference-to-video` with `VIDGO_API_KEY`. Keep model and callback_url at the request root and place workflow parameters inside input.

The response contains a task_id. Use [Query Task Status](/api-manual/task-management/status) or [Webhook Callbacks](/api-manual/task-management/webhooks) to retrieve the files.

## Parameter Details

Required input fields: `prompt`, `reference_image_urls`.

- Send only the fields shown for this workflow. Parameters from other workflows are rejected before credits are deducted.
- When supplied, prompt and text must not be blank. Input URL fields require HTTP(S) URLs with a hostname and no embedded credentials.
- Provide one to seven reference_image_urls. Reference videos belong to the video-edit workflow.

## Developer Notes

Keep the task_id until the task reaches finished or failed. Failed tasks return charged credits through the normal refund process. Existing public IDs remain available; use the exact workflow ID shown on this page for new integrations.

## Optional parameters

Optional input fields: `resolution`, `duration`, `aspect_ratio`. See the request schema for accepted values and defaults.
